# IP máy của bạn trong mạng LAN (WiFi).
# Tìm IP bằng lệnh: ifconfig | grep "inet " | grep -v 127.0.0.1
# Thay YOUR_IP bằng IP thực, ví dụ: 192.168.1.100
EXPO_PUBLIC_SERVER_IP=YOUR_IP
EXPO_PUBLIC_SERVER_PORT=3001